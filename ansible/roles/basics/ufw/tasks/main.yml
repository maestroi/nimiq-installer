- name: Allow everything and enable UFW
  community.general.ufw:
    state: enabled
    policy: allow

- name: Set logging
  community.general.ufw:
    logging: "on"

- name: Allow port 22
  community.general.ufw:
    rule: allow
    port: 22
    proto: tcp
